name: headscale
description: Headscale Tailscale control server
image: headscale/headscale:latest
port: 8080
healthcheck_path: /
category: vpn-network
deploy_mode: standalone
runtime_version: headscale/headscale:latest
