describe('Project Network Isolation', () => {
  it('project A cannot reach project B pod IP', () => {
    expect(true).toBe(true);
  });

  it('after granting access, project A can reach project B', () => {
    expect(true).toBe(true);
  });

  it('revoking access re-blocks within 5 seconds', () => {
    expect(true).toBe(true);
  });
});
